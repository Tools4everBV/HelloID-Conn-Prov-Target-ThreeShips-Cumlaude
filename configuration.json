[
    {
        "key": "UserName",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "UserName",
            "description": "The UserName to connect to the ThreeShhips Cumlaude webservice",
            "required": true
        }
    },
    {
        "key": "Password",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "type": "password",
            "label": "Password",
            "description": "The password to connect to the ThreeShhips Cumlaude webservice",
            "required": true
        }
    },
    {
        "key": "BaseUrl",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "BaseUrl",
            "description": "The URL to the ThreeShhips Cumlaude webservice",
            "placeholder": "https://webservice.threeships.nl",
            "required": true
        }
    },
    {
        "key": "Source",
        "type": "input",
        "defaultValue": "",
        "templateOptions": {
            "label": "Source",
            "description": "The name of the source in which the user will be created",
            "required": true
        }
    },
    {
        "key": "IsDebug",
        "type": "checkbox",
        "defaultValue": true,
        "templateOptions": {
            "label": "Toggle debug logging",
            "description": "When toggled, debug logging will be displayed",
            "required": false
        }
    }
]
